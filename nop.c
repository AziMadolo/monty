#include "monty.h"

/**
 * custom_nop - This function does nothing.
 * @head: Pointer to the stack's head.
 * @counter: Line number in the file.
 *
 * Return: No return value.
 */
void custom_nop(stack_t **head, unsigned int counter)
{
    (void)counter;
    (void)head;
}
